import LinkedList from "./linkedListImpl";

class Stock {
  constructor() {
    (this.current = null), this.deck;
  }

  setDeck(cardList) {}

  drawCard() {}

  takeCurrentCard() {}
}

export default Stock;
